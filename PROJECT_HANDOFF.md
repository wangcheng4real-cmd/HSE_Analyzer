# HSE 数据分析平台项目交接文档

> 更新时间：2026-07-15  
> 项目目录：`D:\HSE_Analyzer`  
> 目标读者：完全没有历史上下文、准备继续开发的新任务/新开发者

## 1. 项目是什么

这是一个基于 Python 3.9、Tkinter、pandas、openpyxl 和 Matplotlib 的桌面端 HSE 数据分析平台。

当前有三个顶层模块：

1. **隐患分析**：读取一个或多个隐患 Excel，按照检查日期形成周、月、季度周期，进行总体、单位、区域、专项和趋势分析。
2. **预警刹车分析**：读取整改通知、通报批评、挂牌督办、管理约谈、停工令等 Excel，统一字段后进行总体、单位、专项和趋势分析。
3. **综合分析**：当前主要作为后续“隐患 + 预警刹车联动、管理驾驶舱、自动预警和自动报表”的承载页面，尚未完成核心业务能力。

软件名称已统一为 **HSE数据分析平台**。

## 2. 当前正在做什么

最近一个需求不是修复现有统计，而是在规划下一阶段的管理能力：

- 从“点击按钮查看图表”升级为面向管理者的风险驾驶舱；
- 分别扩展隐患分析和预警刹车分析的管理指标；
- 建立隐患数据与预警刹车数据的关联模型；
- 增加自动风险识别、自动预警和自动周报/月报/季报；
- 自动告诉管理者“风险在哪里、为什么触发、是否恶化、采取措施后是否有效”。

这一阶段**目前只完成了概念规划，还没有开始编码**。不要误以为自动预警或自动报表已经存在。

建议把未来产品定位为：

> 用隐患作为风险先行指标，用预警刹车作为管理结果指标，按单位、区域、风险类别和时间周期形成可解释的风险点。

## 3. 当前已具备的主要能力

### 3.1 隐患分析

- 单个或多个 Excel 上传；
- 根据绝对路径跳过重复文件；
- 校验“检查日期”；
- 无效日期记录不进入统计，并记录跳过数量；
- 支持自然周、自然月、自然季度；
- 周周期固定为周一至周日；
- 同一周期的多文件数据自动合并；
- 总体统计继续使用完整有效数据，趋势使用分周期数据；
- 已有等级、单位、分类、区域、接口、AB 等总体分析；
- 已有单位画像、区域画像及相关趋势；
- 已有专项隐患趋势、专项隐患（单位）、专项隐患（区域）；
- 专项分类界面包含常用分类和其他二级分类选择；
- 常用分类按实际数量降序显示；
- 已有后台加载、任务锁、缓存和错误日志基础设施。

### 3.2 预警刹车分析

- 支持整改通知、通报批评、挂牌督办、管理约谈、停工令；
- 文件上传时自动识别预警刹车类型；
- 统一形成标准“责任单位”和“发出日期”；
- 支持周、月、季度趋势；
- 已有总体、单位画像、总体趋势、单位趋势；
- 已有预警刹车专项趋势和专项分析（单位）；
- 问题类别可以跨全部预警刹车类型统计；
- 选择单位、区域、问题类别等弹窗已采用统一卡片式 UI；
- 默认按对应记录数量降序排序，支持搜索、滚轮、单击选择、确定、Enter、Esc 和双击快捷确认。

### 3.3 工程基础

- 统一应用状态：`app/core/analysis_state.py`；
- 后台任务机制：`app/core/background_task.py`；
- 统计结果轻量缓存，数据或周期变化时失效；
- 图表创建和图表窗口已独立；
- 页面、控制器、UI 组件和核心服务已经拆分；
- `requirements.txt`、`.gitignore`、`README.md` 已存在；
- 当前自动化测试共 **53项**，2026-07-15 本地执行全部通过。

测试命令：

```powershell
$env:PYTHONUTF8='1'
py -3.9 -m unittest discover -s tests -v
```

## 4. 当前目录和文件职责

```text
D:\HSE_Analyzer
├─ main.py                         程序入口、DPI 初始化、启动主窗口
├─ requirements.txt                运行依赖
├─ README.md                       项目基础使用说明
├─ app
│  ├─ core
│  │  ├─ analyzer.py               顶层分析器组装入口
│  │  ├─ analysis_state.py         隐患/刹车数据状态、周期、版本和缓存
│  │  ├─ background_task.py        单后台任务、进度、取消和主线程回调
│  │  ├─ hazard                    隐患领域加载、预处理、过滤和统计服务
│  │  └─ brake                     预警刹车加载、周期、过滤和统计服务
│  ├─ charts
│  │  ├─ chart.py                  Matplotlib 图表工厂
│  │  └─ font_config.py            中文字体配置
│  └─ ui
│     ├─ main_window.py            主窗口、导航、日志、状态栏和页面组装
│     ├─ theme.py                  颜色、字体、间距和 ttk 样式
│     ├─ pages                     隐患、刹车、综合分析页面布局
│     ├─ controllers               页面业务回调、后台统计和出图协调
│     └─ components                文件表、卡片、选择弹窗、图表窗口等组件
├─ tests                           unittest 自动化测试
└─ logs                            运行错误日志
```

### 4.1 隐患核心文件

- `hazard_data_loader.py`：读取 Excel、日期校验、周/月/季度分组。
- `hazard_config.py`：隐患字段和派生字段配置。
- `hazard_preprocessor.py`：列名、文本、流程、区域和分类预处理。
- `hazard_filter.py`：统一业务筛选。
- `hazard_analyzer.py`：隐患服务组装和对 UI 的统一接口。
- `results.py`：趋势等结果对象，避免 UI 依赖服务内部 DataFrame 细节。
- 其他 `*_service.py`：等级、单位、区域、AB、画像、趋势和专项统计。

### 4.2 预警刹车核心文件

- `brake_data_loader.py`：识别类型、表头、字段别名、责任单位来源和日期来源。
- `brake_period.py`：周/月/季度分组。
- `brake_filter.py`：预警刹车字段清洗和筛选。
- `brake_analyzer.py`：预警刹车服务统一入口。
- `brake_overall_service.py`：总体和常规趋势统计。
- `brake_unit_profile_service.py`：单位画像。
- `brake_special_trend_service.py`：问题类别专项总体/单位趋势。

## 5. 重要数据口径，不能随意改

### 5.1 隐患字段契约

当前主要使用：

- 隐患单号；
- 检查日期；
- 隐患级别；
- 流程类型；
- 状态；
- 责任单位；
- 隐患分类；
- 区域；
- 接口队办；
- 责任班组；
- 验证状态。

派生字段包括：

- 区域大类；
- 隐患类别二级路径；
- 隐患第二级分类；
- 时间周期；
- 周期开始日期。

两种分类口径必须同时保留：

- `施工/用电/临电 → 施工/用电`：用于单位/区域画像的“前两级路径”；
- `施工/用电/临电 → 用电`：用于专项分析的“真正第二级分类名称”。

不要把这两个字段合并，否则画像和专项统计口径都会变化。

### 5.2 隐患时间口径

- 周：自然周，周一至周日；
- 月：自然月；
- 季度：自然季度；
- 文件名和上传顺序不决定时间；
- 必须读取每一行的“检查日期”；
- 多文件同周期数据要合并；
- 趋势排序必须使用“周期开始日期”，不能使用显示文字排序；
- 筛选后没有数据的已存在周期仍需保留并补0。

### 5.3 预警刹车责任单位来源

不同类型不能无条件合并多个候选字段：

| 预警刹车类型 | 标准责任单位来源 |
|---|---|
| 停工令 | 责任单位 |
| 管理约谈/约谈记录 | 被约谈方 |
| 挂牌督办 | 责任承包商 |
| 通报批评 | 责任承包商 |
| 整改通知/整改单 | 责任承包商 |

管理约谈中的被约谈方存在细分名称，当前会按 `/`、`／`、中文括号或英文括号前的内容归并为上级大类单位。调整这条规则前必须先确认真实命名规范。

### 5.4 预警刹车日期来源

| 预警刹车类型 | 日期来源 |
|---|---|
| 整改通知 | 发出日期 |
| 通报批评 | 发出日期 |
| 挂牌督办 | 发出日期 |
| 停工令 | 发出日期 |
| 管理约谈 | 发出时间 |

## 6. 下一阶段建议实施计划

不要直接先写图表。先完成数据治理，再做风险规则，否则联动结果会被单位别名、区域别名和类别名称差异污染。

### 阶段A：确认管理口径和主数据

需要业务方确认：

1. 是否存在正式的单位层级表：上级单位、细分项目部、班组、别名；
2. 是否存在正式的区域层级表；
3. 隐患第二级分类和预警刹车问题类别如何映射为统一风险主题；
4. 五类预警刹车的严重程度排序和权重；
5. 自动报告第一版需要系统页面、Excel、PDF中的哪几种；
6. 是否有计划完成日期、整改完成日期、要求完成日期、施工人数、工时等补充数据。

建议新增配置或数据表：

- `单位主数据`：标准单位、别名、上级单位、单位类型；
- `区域主数据`：一级区域、二级区域、厂房/设施、别名；
- `风险类别映射`：隐患二级分类、预警问题类别、统一风险主题；
- `预警规则配置`：规则名称、阈值、窗口、权重、是否启用。

### 阶段B：管理驾驶舱

隐患驾驶舱优先增加：

- 本周期新增隐患、A/B级、未关闭、验证不通过；
- 环比变化和连续上升；
- 长期未关闭隐患；
- 重复发生的单位/区域/类别组合；
- 风险最高和恶化最快的单位、区域、类别；
- 单位 × 区域 × 类别热力图。

预警刹车驾驶舱优先增加：

- 本周期五类预警刹车数量和构成；
- 未完成、平均处置时间；
- 重复被预警单位；
- 管理措施升级路径；
- 高频问题类别；
- 预警后仍未改善的单位。

### 阶段C：隐患与预警刹车联动

当前两类数据没有可靠的共同业务编号，因此第一版不要声称是“逐条因果关联”。应先按以下标准维度建立时间窗口关联：

```text
标准责任单位 + 标准区域 + 统一风险主题 + 时间周期
```

建议联动逻辑：

- 隐患是风险先行指标；
- 预警刹车是管理结果/措施指标；
- 比较预警前4个周期和预警后4个周期；
- 判断隐患是否下降、A/B级是否下降、是否再次被预警；
- 识别“隐患高但预警低”“隐患和预警都高”“预警后无改善”等管理情形。

### 阶段D：自动风险预警

第一版使用可解释的规则，不要一开始做黑盒 AI 预测。

建议风险点定义：

```text
某个单位/区域/风险类别，在一个周期内因数量、严重程度、增长、重复发生、
未关闭或预警刹车等证据触发的一条管理风险。
```

建议规则：

- 本周期较上一周期增长超过阈值；
- 超过最近8个周期均值一定比例；
- 连续3个周期上升；
- 单周期A/B级数量或占比超阈值；
- 同一单位 + 区域 + 类别连续重复；
- 隐患长期未关闭或验证不通过；
- 单位短期内多次被预警；
- 预警措施逐步升级；
- 预警后同类隐患未下降或反弹；
- 隐患持续高位但没有相应管理措施。

每条风险必须保存：

- 风险对象；
- 风险区域；
- 风险主题；
- 风险等级；
- 触发规则；
- 数据证据；
- 首次发现周期；
- 持续周期数；
- 状态：新增、持续、改善、解除；
- 建议措施；
- 人工确认结果。

### 阶段E：自动报表

建议支持周报、月报、季报，主要结构：

1. 管理摘要：最重要的3～5个结论；
2. 核心指标：隐患、A/B级、未关闭、预警刹车、完成情况；
3. 自动风险清单：风险等级、证据、责任对象、建议措施；
4. 重点单位和区域；
5. 重复问题与管理措施升级；
6. 上期风险点的改善、持续和解除情况；
7. 数据质量说明；
8. 附录图表和明细。

建议实现组件：

- `DimensionMapper`：单位、区域、类别标准化；
- `ManagementMetricService`：管理指标；
- `RiskRuleEngine`：执行规则；
- `RiskScoreService`：风险评分；
- `RiskPointRepository`：保存每期风险点和状态变化；
- `ReportBuilder`：生成 Excel/PDF；
- `RiskDashboardPage`：综合分析页面的管理驾驶舱。

## 7. 自动预警实现原则

### 7.1 风险分数只是排序工具

可以采用100分制，但权重必须可配置并由业务确认。例如：

- 严重程度25；
- 趋势变化20；
- 重复发生15；
- 未关闭/验证15；
- 预警刹车15；
- 管理措施有效性10。

不要把未经确认的权重写死在 Service 中，也不要把分数描述成精确事故概率。

### 7.2 报警必须可解释

错误示例：

> 该单位风险分为82分。

正确示例：

> 该单位风险为红色：临时用电隐患连续3周上升，本周42条，较近8周均值增长85%，其中A/B级6条，且最近4周收到2次整改通知。

### 7.3 保存历史快照

如果只根据当前上传文件实时计算，就无法可靠判断风险是“新增、持续、改善还是解除”。自动预警应保存每次报告的：

- 数据截止日期；
- 使用的文件和数据版本；
- 规则版本；
- 指标结果；
- 风险点结果；
- 人工确认和处置状态。

## 8. 已踩过的坑，务必避免

### 8.1 Tkinter 主线程和后台线程

- pandas 读取和大统计可以在工作线程；
- Tkinter 控件、弹窗和 Matplotlib 窗口只能在主线程创建或更新；
- 工作线程通过线程安全队列传递结果；
- UI 更新必须通过 `root.after()` 回到主线程；
- 同一时间只允许一个大型后台任务；
- 取消加载只能安全地在文件之间检查，不能强杀正在执行的 `read_excel()`；
- 失败或取消不能提交半成品数据。

### 8.2 选择弹窗闪烁、卡死和搜索失效

这些问题历史上反复出现：

- 弹窗先显示空壳，再逐项创建大量卡片，会闪烁；
- 每次打开使用 `bind_all` 但关闭时没有正确解除，第二次打开搜索可能失效或卡死；
- 搜索变量 trace 或事件绑定重复叠加，会触发递归刷新；
- 在弹窗布局期间频繁 `update()` / `update_idletasks()` 会导致中间状态被绘制；
- 主页面 Canvas 与弹窗同时处理滚轮，会造成背景滚动或异常刷新。

继续修改 `RankedSelectionDialog` 时应遵循：

- 每次实例使用独立 `StringVar` 和事件绑定；
- 关闭时解除 trace、滚轮和键盘绑定；
- 避免全局 `bind_all`，确需使用时必须保存并恢复；
- 窗口先隐藏，完成布局后一次显示；
- 不要逐项强制刷新；
- 搜索刷新尽量复用或批量重建卡片；
- 滚轮只作用于鼠标所在的弹窗 Canvas；
- 单击只选择，确定/Enter才提交，双击可快捷确认。

### 8.3 图表窗口默认显示不全或闪烁

- 图表窗口不是越大越好，曾出现默认最大化反而裁切；
- 图表创建、Canvas 布局、窗口显示顺序不正确时，只有手动缩放后才完整；
- 不要在 Figure 尚未获得最终尺寸时反复 `tight_layout()`；
- 先确定窗口客户区尺寸，再创建/布局 Canvas，最后显示并执行一次延迟重绘；
- 不要通过多次显示、隐藏、最大化制造视觉闪烁；
- 所有图表入口应统一使用 `ChartWindow`，不要各 Service 自建 Tk 窗口。

### 8.4 不要在 Service 中直接操作 UI

- Service 只接收数据并返回结构化结果；
- Controller 负责后台任务、缓存、错误处理和调用图表；
- Page 只负责布局、控件状态和用户输入；
- Chart 层只负责根据结果生成 Figure；
- 不要让 Service 弹 `messagebox`、创建 `Toplevel` 或访问 Tkinter 变量。

### 8.5 缓存失效

- 添加、删除、清空文件后，旧 `df_all`、`df_list` 和统计缓存必须失效；
- 成功重新加载后递增数据版本；
- 周/月/季度变化后清空相关缓存；
- 失败或取消时保留上一次成功数据；
- 缓存键必须包含模块、数据版本、周期、分析名称和筛选参数；
- 缓存只存统计结果，不复制完整原始 DataFrame。

### 8.6 Excel 和中文编码

- Windows PowerShell 管道可能把中文脚本、路径或字段名变成 `?`；
- 控制台出现乱码不等于 Python 源文件损坏；
- 不要看到 `Get-Content` 乱码就直接批量重写源文件；
- 运行测试或脚本时优先设置 `$env:PYTHONUTF8='1'`；
- 处理中文路径时优先在 Python 内用 `Path.home()` 和文件枚举，避免经由错误代码页传入中文字符串；
- `openpyxl` 可能提示工作簿没有默认样式，这通常是警告，不代表读取失败；
- 不要为了“加速”盲目使用 `usecols`，除非确认所有文件的字段别名和表头结构都覆盖。

### 8.7 不要混淆业务语义

- 管理约谈的单位必须取“被约谈方”，不是所有候选单位列拼接；
- 不同预警类型的日期来源不同；
- 隐患画像的分类路径和专项二级分类不是一回事；
- 没有共同业务编号时，隐患和预警刹车只能做维度 + 时间窗口关联，不能宣称逐条因果关系；
- 没有计划完成日期时不能准确计算“逾期”，只能计算发现至今或发出至完成时长；
- 没有施工人数/工时/作业量时，单位总量排名不能直接解释为管理水平排名。

### 8.8 当前没有可用 Git 历史

项目目录中可以看到 `.git` 目录，但执行 `git status` 和 `git log` 会提示不是 Git 仓库。新任务不要假设可以使用 Git 恢复，也不要进行 `git reset` 等操作。

开始大改前建议：

1. 先备份项目；
2. 确认 `.git` 当前状态；
3. 如经用户同意，再初始化或修复版本控制；
4. 修改前记录测试基线和关键统计结果。

## 9. 接手后的第一轮工作建议

新任务建议严格按以下顺序执行：

1. 完整阅读本文件、`README.md`、`main.py`；
2. 运行53项测试，确认基线仍然通过；
3. 不启动重构，先向用户确认第6节中的六个管理口径问题；
4. 检查真实 Excel 的字段、缺失率、日期范围和类别取值，只输出聚合结果，不泄露原始业务明细；
5. 形成《管理指标与风险规则字典》，让用户确认公式和阈值；
6. 先实现单位/区域/类别主数据映射；
7. 再实现管理指标服务和综合分析驾驶舱；
8. 然后实现规则型自动预警；
9. 最后实现 Excel/PDF 自动报告；
10. 积累足够历史周期后，再考虑动态基线或预测模型。

## 10. 当前验收基线

2026-07-15 已执行：

```powershell
$env:PYTHONUTF8='1'
py -3.9 -m unittest discover -s tests -v
```

结果：

```text
Ran 53 tests
OK
```

覆盖内容包括：

- 应用状态和缓存；
- 后台任务单任务锁与回调线程；
- 隐患日期、周期和多文件合并；
- 隐患预处理和统计服务；
- 预警刹车类型、单位和日期字段映射；
- 管理约谈单位归并；
- 预警刹车专项趋势；
- 图表工厂；
- 选择弹窗的排序和搜索逻辑；
- 主要模块导入和页面类结构。

测试通过只代表核心逻辑基线正常。Tkinter 的闪烁、DPI、窗口默认尺寸、滚轮和真实 Excel 兼容性仍需要人工视觉和真实数据回归。

## 11. 给下一任务的一句话

**不要直接加一个“自动预警按钮”；先统一单位、区域和风险类别，确认管理规则，再让系统基于可解释证据生成可追踪的风险点。**
